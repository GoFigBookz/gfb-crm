/**
 * AGENT SKILLS — re-exported from the per-agent filing structure in ./skills.
 * Each agent has its own file under api/skills/ (nothing dumped in one place);
 * this module just surfaces the assembled packs for the assistant.
 */
export { skillFor, AGENT_SKILLS } from "./skills";
